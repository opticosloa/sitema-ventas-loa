

export interface Sucursal {
    sucursal_id?: string;
    nombre: string;
    encargado: string;
    direccion: string;
    telefono: string;
    email: string;
    is_active?: boolean;
    created_at?: string;
}
